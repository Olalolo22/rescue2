import { Check, ExternalLink, FileCheck2, GitBranch, ShieldCheck } from 'lucide-react'
const checks = [['I₁', 'No public liquidation before intervention resolves'], ['I₆', 'L1 mutation rejected while position is delegated'], ['I₁₀', 'Settlement produces a durable RescueRecord receipt']]
export default function Verify() { return <main className="site-shell console-page"><Header /><div className="page-heading"><span className="section-label">JUDGE / REVIEWER VERIFICATION</span><h1>Verify the claim.</h1><p>Rescue is built to be checked in minutes: inspect the program, follow the lifecycle, and compare the anchored receipt.</p></div><section className="verify-grid"><div className="verify-card"><span className="section-label">PROGRAM ID</span><code>GCcUbgthDu323rfq9Z3iWNFR632wXWMZ66KKtdTtxDBT</code><a href="https://explorer.solana.com" target="_blank" rel="noreferrer">VIEW DEVNET PROGRAM <ExternalLink size={14} /></a></div><div className="verify-card"><span className="section-label">QUICK VERIFY</span><ol><li><b>01</b> Clone the repository and run the verification harness.</li><li><b>02</b> Inspect the delegation and Error 3007 rejection.</li><li><b>03</b> Compare settlement numbers with the RescueRecord PDA.</li></ol><a href="https://github.com/Olalolo22/rescue2" target="_blank" rel="noreferrer"><GitBranch size={15} /> OPEN REPOSITORY <ExternalLink size={13} /></a></div></section><section className="invariant-panel"><div><span className="section-label">INVARIANTS SCORECARD</span><h2>Three claims.<br /><em>Three checks.</em></h2></div><div className="invariant-list">{checks.map(([id, text]) => <div className="invariant-row" key={id}><span>{id}</span><p>{text}</p><Check /></div>)}</div></section><div className="console-note"><FileCheck2 /><span>See the <a href="/proof/rescue-record-rp-0427">receipt inspector</a> for the concrete settlement proof.</span></div></main> }
function Header() { return <header className="site-nav"><a className="brand" href="/"><span className="brand-mark"><ShieldCheck size={15} /></span><span>RESCUE <b>PROTOCOL</b></span></a><nav className="nav-links"><a href="/demo">DEMO</a><a href="/activity">ACTIVITY</a><a href="/proof/rescue-record-rp-0427">PROOF</a></nav><span className="network-status"><i /> DEVNET</span></header> }
