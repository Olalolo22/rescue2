import { Check, CircleAlert, LockKeyhole, Radio, ShieldCheck } from 'lucide-react'

const events = [['09:42:18.004', 'DELEGATION_COMMITTED', 'Position RP-0427-ALPHA transferred to MagicBlock TEE', 'confirmed'], ['09:42:18.119', 'MEV_MUTATION_REJECTED', 'Error 3007: AccountOwnedByWrongProgram on L1', 'rejected'], ['09:42:19.772', 'BID_COMMITTED', 'Three sealed intervention bids locked inside enclave', 'confirmed'], ['09:42:34.201', 'WINNER_MATCHED', 'Lowest valid penalty selected: Liquidator 03 at 2.50%', 'confirmed'], ['09:42:36.098', 'SETTLEMENT_FINALIZED', 'RescueRecord anchored on Solana L1', 'confirmed']]
export default function Activity() { return <main className="site-shell console-page"><Header /><div className="page-heading"><span className="section-label">LIVE PROTOCOL ACTIVITY</span><h1>Evidence, not theater.</h1><p>Every transition in the intervention lifecycle resolves into a state a judge can inspect.</p></div><section className="activity-shell"><div className="activity-head"><span><i className="telemetry-dot" /> STREAMING / DEVNET</span><span>INCIDENT RP-0427-ALPHA</span></div>{events.map(([time, title, copy, state], index) => <article className="activity-event" key={title}><div className="event-time">{time}</div><div className={`event-marker ${state}`}>{state === 'rejected' ? <CircleAlert /> : index === 4 ? <ShieldCheck /> : <Check />}</div><div><div className="event-title">{title}</div><p>{copy}</p></div><span className={`event-state ${state}`}>{state === 'rejected' ? 'REJECTED' : 'VERIFIED'}</span></article>)}</section><div className="console-note"><LockKeyhole /><span>Activity is a presentation of the Rescue protocol lifecycle. Verify the anchored receipt independently on the <a href="/verify">verification surface</a>.</span></div></main> }
function Header() { return <header className="site-nav"><a className="brand" href="/"><span className="brand-mark"><Radio size={15} /></span><span>RESCUE <b>PROTOCOL</b></span></a><nav className="nav-links"><a href="/demo">DEMO</a><a href="/proof/rescue-record-rp-0427">PROOF</a><a href="/verify">VERIFY</a></nav><span className="network-status"><i /> DEVNET</span></header> }
